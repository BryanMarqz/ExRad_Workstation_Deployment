# Private deployment inputs

Keep organization-specific enrollment inputs in this folder on the technician's
local deployment copy:

- `ninjaone-url.txt`
- `set_gcpw_token.reg`

Both filenames are ignored by Git and must never be committed. Legacy copies at
the project root are still detected for backward compatibility.
