# Private deployment inputs

Keep organization-specific enrollment inputs in this folder on the technician's
local deployment copy:

- `ninjaone-url.txt`

This filename is ignored by Git and must never be committed. A legacy copy at
the project root is still detected for backward compatibility.
